# ✅ Чеклист продакшн-запуска Telegram WebApp ROI

## 🔐 Безопасность и авторизация

- [ ] `SECRET_KEY` установлен из `.env`, отличается от dev
- [ ] JWT содержит `sub`, `email`, `exp` и другие claims
- [ ] Включён `verify_exp=True` в `jwt.decode(...)` на проде
- [ ] CORS ограничен: `allow_origins=["https://web.telegram.org"]`
- [ ] Все защищённые эндпоинты используют `Depends(get_current_advertiser)`
- [ ] Удалены все `console.log(token)` и отладочные логи из `app.js`

---

## ⚙️ Инфраструктура и деплой

- [ ] `BASE_URL` в `app.js` указывает на продакшн-домен
- [ ] Сервер работает через HTTPS (Telegram требует)
- [ ] Настроен `uvicorn`/`gunicorn` с SSL или через Nginx
- [ ] `.env` содержит `DEBUG=False`, `ALLOWED_ORIGINS`, `SECRET_KEY`
- [ ] Reverse proxy (Nginx) корректно перенаправляет WebApp и API

---

## 💬 Telegram WebApp

- [ ] WebApp зарегистрирован в BotFather (`https://yourdomain.com/webapp`)
- [ ] `window.Telegram.WebApp.initData` приходит в `app.js`
- [ ] `/auth/telegram` корректно расшифровывает `initData`
- [ ] WebApp работает внутри Telegram без ошибок
- [ ] Кнопка WebApp добавлена в меню бота или inline-кнопку

---

## 🎨 Фронтенд: `index.html` + `app.js`

- [ ] Удалены все `console.log(...)`, оставлены только `console.error`
- [ ] `applyFilters()` работает без ошибок
- [ ] PNG-график загружается и скачивается
- [ ] Адаптивность проверена на мобильных устройствах
- [ ] Добавлены fallback-сообщения при ошибках API (`403`, `500`, `timeout`)

---

## 🧠 Бэкенд: FastAPI

- [ ] Эндпоинты `/auth/telegram`, `/analytics/roi/daily`, `/plot` возвращают корректные данные
- [ ] `get_current_advertiser` работает с прод-токенами
- [ ] Включено логирование ошибок и запросов (`logging`, `uvicorn.access`)
- [ ] Swagger UI работает с `OAuth2PasswordBearer`
- [ ] SQL-запросы используют `SessionLocal` и закрываются корректно

---

## 📈 Мониторинг и стабильность

- [ ] Включено логирование ошибок и запросов
- [ ] Настроены алерты (Telegram, email, Sentry)
- [ ] Добавлен `/health` endpoint
- [ ] Настроен rate limiting (`slowapi` или аналог)
- [ ] Проведено нагрузочное тестирование

---

## 🎯 Финальные штрихи

- [ ] Добавлена версия WebApp в футер или `console.info("WebApp v1.0.0")`
- [ ] Все поля фильтра работают и валидируются
- [ ] PNG соответствует выбранным фильтрам
- [ ] WebApp протестирован в Telegram на Android, iOS и десктопе
